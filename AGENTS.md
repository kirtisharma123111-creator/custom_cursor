## Cursor Cloud specific instructions

- **Codebase**: Minimal single-file Python repository (`Cusrsor_test.py`).
- **Run**: `python3 Cusrsor_test.py` from the workspace root.
- **Dependencies**: None — only a Python 3.x runtime is required (pre-installed on the VM).
- **Lint/Test/Build**: No linter, test framework, or build system is configured. Run the script directly to verify it works.
